# My-Javascript-course-work
This is my repo containing my new adventure into the world of javascript and react through my coursework
Activity 2.1 - My first piece of JavaScript - Creating an alert function.
Activity 2.2 - Changing HTML content with JavaScript code.
Activity 2.3 - Log some information to the console of the web browser
Activity 2.4 - Practice the syntax of JavaScript concepts - In the most challenging activity so far i inputted - Variables declared with var, let and const of different data types.
Named, anonymous and built in functions  
If/ else and switch conditional statements
Arrays of different data types
Loops through an array. I found it quite challenging just remembering how to write the syntax out, and whilst i seem to understand the concepts and what they are doing on the whole, it will take a while to remember and fully grasp the syntax of JavaScript.
Activity-3.1-A simple banking system - For this project i created a backAccount class with 'properties','methods' for deposit, withdrawals and checking balances. I then ran everything through node.js. I struggled at first with module errors, mainly around using ES modules (fixing the type: "module" setting), import paths and getting node to recognise my files, in the end i had a file path error i noticed and rectified (i forgot to place the .js after a file path). I am still building my confidence with the syntax, but each project is helping the patterns become more familiar.
Activity-4.1-Task list application. This was a simple task list application where users can - add, edit, remove, clear, and mark tasks as completed, also save tasks using local storage so they persist after refreshing page. This task helped me reinforce Javascript concepts such as DOM manipulation, event handlers, arrays, local storage, and i made progress with debugging skills with lots of trial and error and syntax mistakes such as case sensitive mistakes, mistakes in script placement in HTML. I have placed comments on each line in order to help myself get used to what everything is doing, in hopes that i can start remembering it myself as i move forward.
Activity-4.2-Light and dark mode page- had a lot of fun with this one, i made a themed light and dark mode toggle page, you can switch between light and dark mode with the press of a button. i incorporated DOM selection, event handling and class manipulation, and once again basic debugging and testing of the page to get it how i wanted
Activity 5.1-a short javascript exercise that calculates the mean value of a dataset. The script loops through an array of numbers, adds them together, divides by the total amount of values, rounds the result and outputs the final mean.
What i did-stored numeric data in an array, created a function to process values, used a for loop to calculate a running total, returning results from a function and logging output to the console log.
